{
  "name": "master-lab",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "npm --prefix server test",
    "test:audio": "npm --prefix server run test:audio"
  }
}

// Copy to config.js and uncomment ONE option:

// Local API (npm start in server/)
// window.MASTER_LAB_API = "http://localhost:8080";

// Render free tier (see render.yaml in repo root)
// window.MASTER_LAB_API = "https://master-lab-api.onrender.com";

// Hugging Face Docker Space (see server/deploy/huggingface/README.md)
// window.MASTER_LAB_API = "https://YOUR-USERNAME-master-lab-api.hf.space";

// In-browser only (slow on GitHub Pages for long files):
window.MASTER_LAB_API = "";
